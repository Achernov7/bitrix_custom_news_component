# Вопросы

## 1. Инфоблок — по символьному коду или числовому ID?

Сейчас подключено по коду (`IBLOCK_ID => "news"`) — переносимо между окружениями, но визуальный редактор параметров Битрикс кода не знает и при сохранении может сбросить значение. Или это как-то иначе работает? Не было времени разобраться.

## 2. CACHE_FILTER=N — кешировать ли список без фильтров?

Сделал по стандартной семантике Битрикс: `CACHE_FILTER=N` отключает кеш только когда фильтр активен, а список без фильтров кешируется всегда (кеширование же «включено» по ТЗ). Так нормально, или при `N` ожидается полное отключение кеша на всех страницах списка?

Комментариуй про семантику Битрикса выдал клауд.
